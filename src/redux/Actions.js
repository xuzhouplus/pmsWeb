let loginAction = {
	type: 'login',
	payload: {}
}
let logoutAction = {
	type: 'logout'
}

let initAction = {
	type: 'init'
}

let destroyAction = {
	type: 'destroy'
}

let programAction = {
	type: 'program',
	payload: {}
}

export {
	loginAction,
	logoutAction,
	initAction,
	destroyAction,
	programAction
}